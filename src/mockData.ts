import { Order } from './types';

// Los datos demo han sido eliminados del gestor táctico de Calico S.A.
// El sistema ahora se inicializa en estado limpio y espera la importación real de datos del usuario, los cuales persistirán en almacenamiento local.
export const mockOrders: Order[] = [];
